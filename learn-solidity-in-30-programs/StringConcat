//SPDX-License-Identifier: MIT
pragma solidity ^0.8.9;

contract New {
    function join (string memory _a, string memory _b) public pure  returns (string memory) {
        string memory c = string.concat(_a, _b);
        return c;
    }
}
